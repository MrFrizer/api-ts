// Définit une interface ApiResponse représentant la structure d'une réponse générique pour une API
export interface ApiResponse {
    message: string;      // Champ pour le message associé à la réponse
    timestamp: Date;      // Champ pour la date et l'heure de la réponse
}
